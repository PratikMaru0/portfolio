const services = [
  {
    icon: "🕸️",
    title: "Web design",
    desc: "The pad extract crop strategy is an extension of the extract crop strategy. In the extract crop strategy, we were extracting out a smaller area from a larger image. Now, there can be a scenario where the original image is small and we want to extract out a larger area (which is practically not possible without padding). So, the pad extract mode adds a solid colored padding around the image to match the exact size requested.The pad extract crop strategy is an extension of the extract crop strategy. In the extract crop strategy, we were extracting out a smaller area from a larger image. Now, there can be a scenario where the original image is small and we want to extract out a larger area (which is practically not possible without padding). So, the pad extract mode adds a solid colored padding around the image to match the exact size requested.The pad extract crop strategy is an extension of the extract crop strategy. In the extract crop strategy, we were extracting out a smaller area from a larger image. Now, there can be a scenario where the original image is small and we want to extract out a larger area (which is practically not possible without padding). So, the pad extract mode adds a solid colored padding around the image to match the exact size requested.The pad extract crop strategy is an extension of the extract crop strategy. In the extract crop strategy, we were extracting out a smaller area from a larger image. Now, there can be a scenario where the original image is small and we want to extract out a larger area (which is practically not possible without padding). So, the pad extract mode adds a solid colored padding around the image to match the exact size requested. The pad extract crop strategy is an extension of the extract crop strategy. In the extract crop strategy, we were extracting out a smaller area from a larger image. Now, there can be a scenario where the original image is small and we want to extract out a larger area (which is practically not possible without padding). So, the pad extract mode adds a solid colored padding around the image to match the exact size requested.",
    link: "#",
  },
  {
    icon: "📱",
    title: "Mobile app",
    desc: "Web development is the process of building, programming...",
    link: "#",
  },
  {
    icon: "UI",
    title: "UI/ UX design",
    desc: "Web development is the process of building, programming...",
    link: "#",
  },
  {
    icon: "📊",
    title: "Graphics design",
    desc: "Web development is the process of building, programming...",
    link: "#",
  },
  {
    icon: "seo",
    title: "SEO Optimization",
    desc: "Web development is the process of building, programming...",
    link: "#",
  },
];

export default services;
